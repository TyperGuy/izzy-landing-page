import type { NextPage } from 'next';
import LandingPage from '../views/landing/Landing';

const Home: NextPage = () => (
  <div>
    <LandingPage/>
    Raimundo Tony
  </div>
);

export default Home;
